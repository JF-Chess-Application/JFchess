<h3>Join a Game</h3>

<%= simple_form_for @game do |f| %>
  <%= f.input :name, required: false, label: 'Give this game a name' %>
  <%= f.input :opponent, required: true, label: 'Sign yourself up as the opponent', value: current_user %>
  <%= f.button :submit, "Join the game!" %>
<% end %>